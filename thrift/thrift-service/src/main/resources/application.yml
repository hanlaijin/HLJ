spring:
    datasource:
        url: jdbc:mysql://localhost:3306/wifi?useUnicode=true&characterEncoding=utf-8
        username: root
        password: hlj
    jpa:
        database: MYSQL
        show-sql: true
        hibernate:
            ddl-auto: update
            naming-strategy: org.hibernate.cfg.ImprovedNamingStrategy
        properties:
            hibernate:
                dialect: org.hibernate.dialect.MySQL5Dialect